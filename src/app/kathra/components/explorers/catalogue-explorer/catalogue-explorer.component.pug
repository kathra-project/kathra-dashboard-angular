.header
  .left.control <
  .title CATALOG
  .right.control >
.content
  .tabs
    .tab.search.selected SEARCH
    .tab-splitter
    .tab.pinned PINNED
  .tabs-content
    .tab-content.search.selected
      .sgm2.icon.input
        input(type="text" placeholder="Filter Applications..." , [(ngModel)]="filterValue")
        i.search.icon
      .select-all(title="Select all")
        .wrapper(style="position: relative; z-index: 2")
          .selector.all-apps
            input(type="checkbox" id="expl_all_apps_check", (change)="updateSelection($event, 'ALL')")
            label(for="expl_all_apps_check")
      .apps
        .app(*ngFor="let app of catalogEntries")
          kathra-app([appId]="app.providerId", [catalogEntry]="app", [display]="'compact'")
          .selector
            input(type="checkbox" id="{{'expl_' + app.providerId + '_check'}}", (change)="updateSelection($event, app.providerId)")
            label(for="{{'expl_' + app.providerId + '_check'}}")
    .tab-content.pinned

  .button.add((click)="addSelectedAppsToEnv()") ADD TO ENVIRONMENT